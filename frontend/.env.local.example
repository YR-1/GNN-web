# Frontend environment variables — copy to frontend/.env.local and fill in real values.
# Do NOT commit the real frontend/.env.local (it is gitignored).

NEXT_PUBLIC_SUPABASE_URL=https://<project_ref>.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=<supabase_publishable_or_anon_key>
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
